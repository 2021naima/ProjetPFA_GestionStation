package pfaProject.gestionStation.service;

import pfaProject.gestionStation.entities.tache;
import pfaProject.gestionStation.entities.toDo;
import pfaProject.gestionStation.repositories.toDoRepository;

import java.time.LocalDate;
import java.util.Calendar;

import java.util.List;

public class serviceToDo {
 /*   public toDoRepository toDoRepository;
     public toDo createToDo(List<tache> listTaches){
         toDo todo=new toDo();
         todo.setListTaches(listTaches);
         todo.setDate(LocalDate.ofEpochDay(Calendar.DATE));
         toDoRepository.save(todo);
        return todo;
    }
*/

}
